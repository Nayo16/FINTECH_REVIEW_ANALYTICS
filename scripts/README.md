# Scripts Directory

This folder is intended for executable scripts that support the analytics pipeline.

- `src/review_pipeline.py` contains the scraper and preprocessing logic.

Run the scraper with:

```powershell
python -m src.review_pipeline
```
